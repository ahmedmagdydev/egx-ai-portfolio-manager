# Verified toolchain

These versions were recorded from commands run on the Linux VM during Phase 00:

```text
git version 2.34.1
v20.18.1
10.8.2
Python 3.11.11
Docker version 27.4.1, build b9d17ea
Docker Compose version v2.32.1
```

The `nvidia-smi` and `ollama` commands are unavailable on this VM (`command not found`), so the GPU and Ollama checklist items from the Windows-oriented source runbook are unverified. This phase does not require either dependency.
